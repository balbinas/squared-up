<div class="rectangle"
  id={{@id}}
  style="position: absolute;
         left:{{@startX}}px;
         top:{{@startY}}px;
         width:{{this.width}}px;
         height:{{this.height}}px;
         background-color:{{@color}};">
</div>